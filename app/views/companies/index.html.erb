<div id="featured-companies" class="row section wow fadeInUp" data-wow-duration="2s" data-wow-delay="3s">
  <h2>Featured Companies</h2>
  <h3>Champions of Diversity</h3>
  <% if @companies.any? %>
    <% @companies.each do |company| %>
      <div class="col m3 top-company">
        <h5><%= link_to  company.name , company_path(company)%></h5>
        <img class="company-img" src="<%= company.profile_image_url %>" alt="<%= company.name %>" />
        <span class="rating"><%= custom_rating_generator company.avg_rating %></span>
      </div>
    <% end %>
  <% end %>
</div>

<div class="row section">
  <% if current_user && current_user.admin == true %>
    <%= link_to "Create Company", new_company_path %>
  <% end %>
</div>

<div class="company-data" data="<%= autocomplete_data %>"></div>
